# Business Logic of PAREDIFA Implemented in Prolog

This project performs 2 main tasks:
  * It takes a subset of Regex and translates it into a DFA (deterministic finite automaton).
  * It takes a DFA and evaluates it.
